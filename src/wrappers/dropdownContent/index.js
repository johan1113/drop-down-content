import React from 'react';
import Modal from 'react-modal';

const DropDownContentWrapper = ({ displayDropDownContent, actionUpdateState }) => {

    return (
        <main>
            <Modal>
            </Modal>
        </main>
    );
};

export default DropDownContentWrapper;